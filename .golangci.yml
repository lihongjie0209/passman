version: "2"

run:
  timeout: 5m

linters:
  default: standard
  enable:
    - bodyclose
    - errcheck
    - gosec
    - govet
    - ineffassign
    - nilerr
    - staticcheck
    - unused

formatters:
  enable:
    - gofmt
    - goimports
